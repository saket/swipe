pluginManagement {
  repositories {
    google()
    mavenCentral()
    gradlePluginPortal()
  }
}

dependencyResolutionManagement {
  repositories {
    google()
    mavenCentral()
  }
}

include ':library'
include ':sample'

rootProject.name = "swipe"
enableFeaturePreview('TYPESAFE_PROJECT_ACCESSORS')
